import 'package:flutter/material.dart';

class GraficoLineal extends StatelessWidget {
  const GraficoLineal({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
      color: Colors.grey[300],
      child: Center(child: Text('Gráfico Lineal Aquí')),
    );
  }
}
